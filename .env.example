# Database Configuration
# Choose one of the following based on your setup:

# Option 1: SQLite for local development (default)
DATABASE_URL=file:./data/tech-upkeep.db

# Option 2: PostgreSQL running in Docker (from host machine)
# Run: docker compose up -d
# DATABASE_URL=postgresql://postgres:postgres@localhost:5434/tech_upkeep

# Resend Email Service
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Discord Webhook for Blog Visit Logging (Optional)
# Create a webhook in your Discord server: Server Settings -> Integrations -> Webhooks
# The URL should look like: https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
DISCORD_WEBHOOK_URL=

# Optional: YouTube API for video aggregation
# Get from https://console.cloud.google.com/
YOUTUBE_API_KEY=

# OpenAI API for AI features (script generation, categorization)
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=

# AI Video Generation (Optional)
# ElevenLabs for voice synthesis - Get from https://elevenlabs.io/
# NOTE: Free alternative (StreamElements TTS) is used automatically if not set
ELEVENLABS_API_KEY=

# Video generation options (in order of recommendation):
# 1. SadTalker via Gradio (FREE, no API key needed) - Default, works out of the box
# 2. HuggingFace Spaces - Get from https://huggingface.co/settings/tokens (optional)
HUGGINGFACE_API_KEY=

# 3. Replicate for production (paid) - Get from https://replicate.com/account/api-tokens
REPLICATE_API_KEY=

# Node Environment
NODE_ENV=development
